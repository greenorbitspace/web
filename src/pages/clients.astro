---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import HeroSection from '../components/HeroSection.astro';
import OrganisationsList from '../components/OrganisationsList.jsx';
import allOrganisations from '../data/organisations.json';

// Filter to clients only (still server-side)
const clients = allOrganisations.filter(org => org.Type === 'client');

// Sort alphabetically
const sortedClients = clients.slice().sort((a, b) =>
  (a.Organisation ?? '').localeCompare(b.Organisation ?? '')
);
---

<Layout title="Clients" description="Explore our strategic and delivery clients in the space sector.">
  <Header />

  <main class="py-12 max-w-6xl mx-auto px-4">
    <HeroSection 
      title="Clients" 
      description="Explore our strategic and delivery clients in the space sector." 
    />

    <!-- Hydrated client-side component -->
    <section class="mt-8">
      <OrganisationsList organisations={sortedClients} client:load />
    </section>
  </main>

  <Footer />
</Layout>