---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import ShareButtons from '../../components/ShareButtons.astro';
import InsightsGrid from '../../components/InsightsGrid.jsx';
import { getCollection } from 'astro:content';
import { SDGs } from '../../data/sdgs';
import crypto from 'crypto';
import { slug as slugify } from 'github-slugger';

// --- Static paths ---
export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map(post => ({ params: { slug: post.slug ?? post.id } }));
}

// --- Runtime ---
const { slug: paramSlug } = Astro.params;
const posts = await getCollection('blog');

// Find post by slug, id, or slugified title
let postEntry = posts.find(p => p.slug === paramSlug || p.id === paramSlug)
  ?? posts.find(p => slugify(p.data.title).toLowerCase() === paramSlug?.toLowerCase());
if (!postEntry) throw new Error(`No blog article found for slug: "${paramSlug}"`);

const slug = postEntry.slug || slugify(postEntry.data.title).toLowerCase();
const { Content } = await postEntry.render();
const data = postEntry.data;

// --- Author ---
const team = await getCollection('team');
const authorSlug = slugify(data.author || '').toLowerCase();
const authorInfo = team.find(a => slugify(a.data.title || a.data.slug || '').toLowerCase() === authorSlug) || null;

function getGravatarUrl(email, size = 80) {
  if (!email) return null;
  const hash = crypto.createHash('md5').update(email.trim().toLowerCase()).digest('hex');
  return `https://www.gravatar.com/avatar/${hash}?s=${size}&d=identicon`;
}

const avatarUrl = authorInfo?.data?.contactEmail
  ? getGravatarUrl(authorInfo.data.contactEmail)
  : authorInfo?.data?.image || '/images/default-author.png';

const formattedDate = data.pubdate
  ? new Date(data.pubdate).toLocaleDateString('en-GB', { year: 'numeric', month: 'long', day: 'numeric' })
  : null;

// --- Page metadata ---
const pageTitle = data.seoTitle || data.title;
const pageDescription = data.seoDescription || data.description || data.title;
const pageURL = `https://greenorbit.space/blog/${slug}`;
const pageImage = data.featuredImage || '/default-og-image.jpg';

// --- SDG Map & Codes ---
const sdgMap = SDGs.reduce((acc, sdg) => {
  acc[`SDG ${String(sdg.id).padStart(2, '0')}`.toUpperCase()] = sdg;
  return acc;
}, {});

const sdgCodes = Array.isArray(data.SDGs)
  ? data.SDGs.map(n => `SDG ${String(n).padStart(2,'0')}`)
  : typeof data.SDGs === 'number'
  ? [`SDG ${String(data.SDGs).padStart(2,'0')}`]
  : [];

// --- Related posts ---
const relatedPosts = posts
  .filter(p => p.slug !== slug)
  .map(p => {
    const tags = p.data.tags ?? [];
    const postSDGs = Array.isArray(p.data.SDGs)
      ? p.data.SDGs.map(n => `SDG ${String(n).padStart(2,'0')}`)
      : typeof p.data.SDGs === 'number'
      ? [`SDG ${String(p.data.SDGs).padStart(2,'0')}`]
      : [];

    const tagScore = data.tags ? tags.filter(t => data.tags.includes(t)).length : 0;
    const sdgScore = sdgCodes.filter(code => postSDGs.includes(code)).length;

    const authorsArray = Array.isArray(p.data.author) ? p.data.author : [p.data.author];
    const authorMatch = authorsArray.some(a => slugify(a || '').toLowerCase() === authorSlug) ? 5 : 0;

    const recencyScore = p.data.pubdate ? new Date(p.data.pubdate).getTime() : 0;
    const score = tagScore * 3 + sdgScore * 2 + authorMatch + recencyScore * 1e-9;

    // Map author info for related post
    const postAuthorSlug = authorsArray[0] ? slugify(authorsArray[0]).toLowerCase() : null;
    const postAuthorInfo = team.find(a => slugify(a.data.title || a.data.slug || '').toLowerCase() === postAuthorSlug) || null;
    const postAvatarUrl = postAuthorInfo?.data?.contactEmail
      ? getGravatarUrl(postAuthorInfo.data.contactEmail)
      : postAuthorInfo?.data?.image || '/images/default-author.png';

    return {
      title: p.data.title,
      slug: p.slug,
      url: `/blog/${p.slug}`,
      excerpt: p.data.excerpt,
      date: p.data.pubdate ? new Date(p.data.pubdate).toLocaleDateString('en-GB', { year: 'numeric', month: 'long', day: 'numeric' }) : '',
      tags: p.data.tags ?? [],
      featuredImage: p.data.featuredImage || '/images/placeholder.jpg',
      organisations: (p.data.organisations || []).map(o => ({ name: o.name, slug: o.slug, url: `/organisations/${o.slug}` })),
      SDGs: (Array.isArray(p.data.SDGs) ? p.data.SDGs : []).map(id => sdgMap[`SDG ${String(id).padStart(2,'0')}`.toUpperCase()]).filter(Boolean),
      author: postAuthorInfo ? { name: postAuthorInfo.data.title, slug: postAuthorInfo.slug, avatar: postAvatarUrl } : null,
      score
    };
  })
  .sort((a,b) => b.score - a.score)
  .slice(0,3);

// --- Structured Data ---
const jsonLd = {
  "@context": "https://schema.org",
  "@type": "Article",
  mainEntityOfPage: pageURL,
  headline: pageTitle,
  image: [pageImage],
  datePublished: data.pubdate || new Date().toISOString(),
  dateModified: data.lastModified || data.pubdate || new Date().toISOString(),
  author: authorInfo ? { "@type": "Person", name: authorInfo.data.title, url: `/authors/${authorInfo.slug}` } : undefined,
  publisher: { "@type": "Organization", name: "Green Orbit Digital", logo: { "@type": "ImageObject", url: "https://greenorbit.space/images/logo.png" } },
  description: pageDescription
};
---

<Layout title={pageTitle} description={pageDescription} canonical={pageURL}>
  <Header />

  <!-- Hero Section -->
  <section class="relative w-full h-[500px] sm:h-[600px] lg:h-[700px] flex items-center justify-center overflow-hidden">
    <div class="absolute inset-0 bg-cover bg-center transition-transform duration-500" style={`background-image: url(${pageImage});`} aria-hidden="true"></div>
    <div class="absolute inset-0 bg-black/50" aria-hidden="true"></div>
    <div class="relative z-10 text-center px-4 sm:px-6 lg:px-8 max-w-6xl space-y-6">
      <h1 class="text-5xl sm:text-6xl lg:text-7xl font-extrabold text-white drop-shadow-lg">{data.title}</h1>
      {data.description && <p class="text-2xl sm:text-3xl text-gray-200 drop-shadow">{data.description}</p>}

      <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mt-4 flex-wrap">
        {authorInfo && (
          <a href={`/authors/${authorInfo.slug}`} class="inline-flex items-center gap-2 bg-gray-900/60 text-white px-4 py-2 rounded-full font-semibold shadow-lg hover:bg-gray-800 transition">
            <img src={avatarUrl} alt={authorInfo.data.title} class="w-8 h-8 rounded-full" loading="lazy" />
            <span>By {authorInfo.data.title}</span>
          </a>
        )}
        {formattedDate && <span class="inline-block bg-accent-500 text-white px-4 sm:px-6 py-2 rounded-full font-semibold text-sm sm:text-base shadow-lg">{formattedDate}</span>}
        {data.tags?.length > 0 && (
          <div class="flex flex-wrap gap-2 justify-center">
            {data.tags.map(tag => <a key={tag} href={`/blog?tag=${encodeURIComponent(tag)}`} class="inline-block bg-accent-500 text-white px-4 sm:px-6 py-2 rounded-full font-semibold text-sm sm:text-base shadow-lg hover:bg-accent-600 transition">#{tag}</a>)}
          </div>
        )}
      </div>

      {sdgCodes.length > 0 && (
        <div class="flex flex-wrap gap-4 mt-6 justify-center">
          {sdgCodes.map(code => {
            const sdg = sdgMap[code];
            if (!sdg) return null;
            return (
              <a key={code} href={`https://sdgs.greenorbit.space/${sdg.id}/`} target="_blank" rel="noopener noreferrer" aria-label={`View details about ${sdg.name}`} class="group">
                <img src={sdg.icon} alt={sdg.name} class="w-12 h-12 rounded-sm border border-gray-300 dark:border-gray-600 hover:opacity-80 transition" loading="lazy" />
              </a>
            );
          })}
        </div>
      )}
    </div>
  </section>

  <!-- Main content -->
  <main class="py-12 w-full max-w-[1600px] mx-auto px-4 sm:px-6 lg:px-8 space-y-8">
    <article class="prose prose-lg dark:prose-invert max-w-none">
      <Content />
    </article>

    <section class="mt-12 bg-accent-600 rounded-lg p-8 text-center text-white shadow-lg">
      <h2 class="text-2xl sm:text-3xl font-bold mb-4">Enjoyed this insight?</h2>
      <p class="mb-6">Subscribe to our blogletter for updates on sustainable space operations and research.</p>
      <a href="/subscribe" class="inline-block bg-white text-accent-600 font-semibold px-6 py-3 rounded-md hover:bg-gray-100 transition">Subscribe Now</a>
    </section>

    <!-- Related posts -->
    {relatedPosts.length > 0 && (
      <section class="w-full py-12">
        <h2 class="text-2xl font-bold mb-6 text-center text-accent-500">Related Posts</h2>
        <InsightsGrid posts={relatedPosts} />
      </section>
    )}
  </main>

  <ShareButtons url={pageURL} title={pageTitle} />
  <Footer />

  <script type="application/ld+json">{JSON.stringify(jsonLd)}</script>
</Layout>